"use strict"

module.exports = () => {
  // TODO: ask for new version
  // TODO: run `npm version <action> --preid <preid>`
  //           `pnpm ... (the same)`
  //           `yarn version --<action> --preid <preid>`
  // TODO: run `git push && git push --tags`
  // TODO: run `npm publish --access public`
}
